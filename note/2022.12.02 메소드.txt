int 등은 지역변수인데 같은 이름을 사용 가능하다
JVM은 무조건 main 함수만 실행한다

모든 파일에서 호출가능하게 할거면 public
내 파일에서만 호출 가능하게 하려면 private
method는 class에만 담기 가능

private, public을 쓰지 않으면 같은 패키지 내에서만 사용 가능
static method랑 일반 method랑 나뉨

생성자도 메소드기 때문에 오버로딩 가능
클래스에 속성,생성자,메소드,세터앤게터 포함

클래스명은 대문자 / 변수명은 소문자(합성어는 중간에 대문자 가능) --> 대문자는 3번째 글자부터