깃허브 설정 후 폴더 cmd
git add . => git commit -m "java_ch01" => git push origin main

다른 피시에서는 git pull origin main

--------------------------------------------------------------------------

자바 패키지 ex) com.lec.ex 
         네임 ex) VarEx01 --> 클래스명(소문자)

Run : ctrl + F11 --> 콘솔에 출력
변수 이름은 소문자 알파벳으로 입력{특수문자는 _(언더바)만 사용가능}

주석처리 : ctrl + / (한줄 주석) ---- shift + / (전체주석)

빨간줄(에러) : 문구 및 선언X